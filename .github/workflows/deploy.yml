name: Deploy a aapanel.

on:
  push:
    branches:
      - dev
      - qa
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}

      - name: Setear la carpeta de destino
        run: |
          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            echo "TARGET_DIR=/www/wwwroot/minuevositio.com" >> $GITHUB_ENV
          elif [ "${GITHUB_REF_NAME}" = "qa" ]; then
            echo "TARGET_DIR=/www/wwwroot/planesqa.com" >> $GITHUB_ENV
          elif [ "${GITHUB_REF_NAME}" = "dev" ]; then
            echo "TARGET_DIR=/www/wwwroot/planesdev.com" >> $GITHUB_ENV
          fi

      - name: Deploy con rsync
        run: |
          rsync -av --delete --no-group --no-perms --no-owner --omit-dir-changes --ignore-errors \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='.user.ini' \
            ./ $TARGET_DIR/
